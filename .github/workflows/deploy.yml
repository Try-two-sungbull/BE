name: Deploy to Coolify

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Coolify Deploy
        run: |
          curl -sS -L "${{ secrets.COOLIFY_DEPLOY_WEBHOOK }}"
